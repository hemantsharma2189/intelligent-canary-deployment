apiVersion: v1
kind: Service
metadata:
  name: canary-app
  labels:
    app: canary-app
spec:
  selector:
    app: canary-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
